name: Build Firefox XPI 1.81.14

on:
  push:
    branches:
      - firefox-edge-read-aloud
    paths:
      - '.github/workflows/build-firefox-18114.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: firefox-edge-read-aloud

      - name: Apply sentence highlighting fix
        run: python3 .github/scripts/fix18114.py

      - name: Validate source
        shell: bash
        run: |
          node --check js/firefox-page.js
          node --check js/firefox-chatgpt.js
          node --check js/firefox-chatgpt-background.js
          node --check js/firefox-tts.js
          test "$(jq -r .version manifest.json)" = "1.81.14"
          grep -q 'speech.engine === "Piper" || speech.engine === "Supertonic"' js/firefox-page.js
          grep -q "info.speech.engine === 'Piper' || info.speech.engine === 'Supertonic'" js/firefox-chatgpt.js
          grep -q 'rangeForSourceSpan' js/firefox-page.js
          grep -q 'rangeForTurnSourceSpan' js/firefox-chatgpt.js
          grep -q 'mappedSourceSpan' js/firefox-page.js
          grep -q 'mappedSourceSpan' js/firefox-chatgpt.js
          if grep -RIn --include='*.js' 'innerHTML[[:space:]]*=' js | grep -v 'jquery'; then
            echo 'First-party innerHTML assignment reintroduced'
            exit 1
          fi

      - name: Commit 1.81.14 source
        shell: bash
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add js/firefox-page.js js/firefox-chatgpt.js manifest.json
          git commit -m 'Highlight Piper and Supertonic by sentence'
          git push origin HEAD:firefox-edge-read-aloud

      - name: Build Firefox XPI
        shell: bash
        run: |
          mkdir -p build
          zip -r build/read-aloud-firefox-1.81.14.xpi _locales css icons img js sound *.html manifest.json
          unzip -t build/read-aloud-firefox-1.81.14.xpi
          unzip -p build/read-aloud-firefox-1.81.14.xpi manifest.json | jq -e '.version == "1.81.14"'

      - name: Upload Firefox XPI
        uses: actions/upload-artifact@v4
        with:
          name: read-aloud-firefox-1.81.14
          path: build/read-aloud-firefox-1.81.14.xpi
          if-no-files-found: error
          compression-level: 0
